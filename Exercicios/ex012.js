// Logica de programação 02:

// Escreva uma função chamada ePaisagem que recebe 2 argumentos, largura e altura de uma imagem.
// Retorne true se for uma paisagem:

const ePaisagem = (larg, alt) => console.log(larg > alt);

ePaisagem(200, 1000);
ePaisagem(200, 100);