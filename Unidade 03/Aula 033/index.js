// For of

// Apenas para objetos interaveis (que possuem índice).

// Não precisa entregar o índice:
const pessoa = 'Gustavo';
for (c of pessoa) {
    console.log(c); // G U S T A V O
}

// Caso não use objeto interavel dá TypeERROR.